interface:
  display_name: "Agent Hive"
  short_description: "Coordinate agents with conditional commits"
  default_prompt: "Use $agent-hive to coordinate this task with subagents, verification gates, conditional checkpoint commits, and a self-contained .agent-hive/PLAN-PROGRESS file."
